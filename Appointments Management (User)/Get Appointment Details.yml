info:
  name: Get Appointment Details
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/api/v1/appointments/1"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get details of a specific appointment
