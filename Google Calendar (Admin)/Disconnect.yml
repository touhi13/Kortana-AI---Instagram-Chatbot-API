info:
  name: Disconnect
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/v1/admin/google-calendar/disconnect"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{admin_auth_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Disconnect Google Calendar integration
