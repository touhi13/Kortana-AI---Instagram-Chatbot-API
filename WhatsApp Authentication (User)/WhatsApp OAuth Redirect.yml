info:
  name: WhatsApp OAuth Redirect
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/v1/auth/whatsapp"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

