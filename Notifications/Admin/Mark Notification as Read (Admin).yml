info:
  name: Mark Notification as Read (Admin)
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/api/v1/shared/notifications/12/mark-read"
  headers:
    - name: Authorization
      value: Bearer {{admin_auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Admin: Mark a specific notification as read"
