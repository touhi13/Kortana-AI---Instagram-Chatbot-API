info:
  name: Delete Notification (User)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/api/v1/shared/notifications/12"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "User: Delete a specific notification"
