info:
  name: Get Conversation Detail
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/v1/conversations/1"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a single conversation details by ID
