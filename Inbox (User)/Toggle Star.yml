info:
  name: Toggle Star
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/api/v1/conversations/1/star"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Toggle star state for a conversation by ID
