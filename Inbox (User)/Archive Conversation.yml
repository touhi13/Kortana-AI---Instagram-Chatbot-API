info:
  name: Archive Conversation
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/api/v1/conversations/1/archive"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Archive a conversation by ID
