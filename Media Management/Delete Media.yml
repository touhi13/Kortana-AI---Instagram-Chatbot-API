info:
  name: Delete Media
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/api/v1/media/1"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{auth_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete a media file
