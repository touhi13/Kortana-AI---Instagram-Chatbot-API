info:
  name: Refund
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8000/api/v1/admin/payments/{userSubscriptionId}/refund
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
