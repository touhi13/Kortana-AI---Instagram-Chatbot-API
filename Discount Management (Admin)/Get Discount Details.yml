info:
  name: Get Discount Details
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/v1/admin/discounts/:discount"
  headers:
    - name: Authorization
      value: Bearer {{admin_auth_token}}
    - name: Accept
      value: application/json
  params:
    - name: discount
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
