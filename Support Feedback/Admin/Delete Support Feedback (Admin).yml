info:
  name: Delete Support Feedback (Admin)
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/api/v1/admin/support-feedback/1"
  headers:
    - name: Authorization
      value: Bearer {{admin_auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Admin: Delete support feedback by ID"
