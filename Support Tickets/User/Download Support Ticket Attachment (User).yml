info:
  name: Download Support Ticket Attachment (User)
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/v1/support-ticket/attachment/1/download"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "User: Download support ticket attachment"
