info:
  name: Get Support Ticket Details (Admin)
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/admin/support-ticket/1"
  headers:
    - name: Authorization
      value: Bearer {{admin_auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Admin: Get details of a specific support ticket"
