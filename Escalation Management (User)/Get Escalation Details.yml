info:
  name: Get Escalation Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/escalations/1"
  headers:
    - name: Authorization
      value: Bearer {{auth_token}}
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get detailed information about a specific escalation
