info:
  name: Toggle Package Status
  type: http
  seq: 7

http:
  method: PUT
  url: "{{base_url}}/api/v1/admin/packages/1/toggle-status"
  headers:
    - name: Authorization
      value: Bearer {{admin_auth_token}}
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Toggle package active status (activate/deactivate)
